const {test,expect}=require('@playwright/test');
const BASE='http://127.0.0.1:4173/index.html';
async function ready(page){await page.goto(BASE,{waitUntil:'domcontentloaded'});await expect(page.getByRole('heading',{name:'Learn Your Lines'})).toBeVisible();await page.waitForFunction(()=>MTS_INDEX_ZERO?.store?.hasCore?.(),null,{timeout:12000})}
test('UI chrome is English and pull-to-refresh overscroll is disabled',async({page})=>{await ready(page);await expect(page.getByRole('button',{name:/Open Act I · Scene I/})).toBeVisible();await page.goto(BASE+'#/more');await expect(page.getByRole('heading',{name:'Choose Your Role'})).toBeVisible();const x=await page.evaluate(()=>({html:getComputedStyle(document.documentElement).overscrollBehaviorY,body:getComputedStyle(document.body).overscrollBehaviorY,lang:document.documentElement.lang}));expect(x).toEqual({html:'none',body:'none',lang:'en'})});
test('study notes precede collapsed Structure when grammar exists',async({page})=>{await ready(page);const target=await page.evaluate(async()=>{await MTS_INDEX_ZERO.store.loadStudy();for(const scene of ['act1-scene1','act1-scene2','act2'])for(const speech of MTS_INDEX_ZERO.store.getScene(scene))if(MTS_INDEX_ZERO.store.getGrammar(speech.id).length)return{scene,line:speech.id};return null});expect(target).toBeTruthy();await page.goto(`${BASE}#/line?scene=${target.scene}&line=${target.line}`);await page.waitForFunction(()=>MTS_INDEX_ZERO.store.hasStudy()&&MTS_INDEX_ZERO.store.hasStructure(),null,{timeout:15000});await expect(page.getByText('Grammar / Usage',{exact:true})).toBeVisible();const order=await page.evaluate(()=>[...document.querySelectorAll('.line-page > .card')].map((c,i)=>({i,t:c.textContent})));const gi=order.find(x=>x.t.includes('Grammar / Usage')).i,si=order.find(x=>x.t.includes('Structure')).i;expect(gi).toBeLessThan(si);const details=page.locator('.structure-details');await expect(details).not.toHaveAttribute('open','');await page.locator('.structure-summary').click();await expect(details).toHaveAttribute('open','')});
test('dictionary sheet closes with a fast downward flick from a scrolled position',async({page})=>{await ready(page);const target=await page.evaluate(async()=>{await MTS_INDEX_ZERO.store.loadStudy();for(const s of ['act1-scene1','act1-scene2','act2'])for(const x of MTS_INDEX_ZERO.store.getScene(s))if(MTS_INDEX_ZERO.store.getVocabulary(x.id).length)return{scene:s,line:x.id};return null});await page.goto(BASE+'#/line?scene='+target.scene+'&line='+target.line);await page.waitForFunction(()=>!!document.querySelector('[data-detail-word]'),null,{timeout:15000});await page.locator('[data-detail-word]').first().click();await expect(page.locator('#word-overlay')).toBeVisible();await page.evaluate(()=>{const s=document.querySelector('.word-sheet');s.scrollTop=120;s.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,pointerType:'pen',clientX:180,clientY:220}));s.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,pointerType:'pen',clientX:182,clientY:350}))});await expect(page.locator('#word-overlay')).toBeHidden({timeout:1000})});
test('line focus swipes between speeches, resets scroll, and highlights the selected role',async({page})=>{await ready(page);const target=await page.evaluate(async()=>{await MTS_INDEX_ZERO.store.loadStudy();for(const scene of ['act1-scene1','act1-scene2','act2']){const rows=MTS_INDEX_ZERO.store.getScene(scene);for(let i=1;i<rows.length-1;i++)if(rows[i].speaker==='MOLLIE'&&rows[i+1].speaker!=='MOLLIE'&&(MTS_INDEX_ZERO.store.getGrammar(rows[i].id).length||MTS_INDEX_ZERO.store.getVocabulary(rows[i].id).length))return{scene,line:rows[i].id,next:rows[i+1].id};}return null});expect(target).toBeTruthy();await page.evaluate(t=>{MTS_INDEX_ZERO.state.setRole('MOLLIE');location.hash=`#/line?scene=${encodeURIComponent(t.scene)}&line=${encodeURIComponent(t.line)}`},target);await expect(page.locator('.speaker-title')).toHaveText('MOLLIE');await page.waitForFunction(()=>document.querySelector('.line-page>.card')?.dataset.ownRole==='true');await expect(page.locator('.line-page>.card').first()).toHaveClass(/selected-role-line/);await expect(page.locator('.line-page>.card').first()).toHaveClass(/focus-role-line/);await page.waitForFunction(()=>document.documentElement.scrollHeight>innerHeight+80,null,{timeout:15000});await page.evaluate(()=>window.scrollTo(0,document.documentElement.scrollHeight));expect(await page.evaluate(()=>scrollY)).toBeGreaterThan(20);await page.evaluate(()=>{const p=document.querySelector('.line-page');p.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,pointerType:'touch',pointerId:41,clientX:310,clientY:260}));p.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,pointerType:'touch',pointerId:41,clientX:110,clientY:266}))});await expect(page).toHaveURL(new RegExp(target.next));await page.waitForFunction(()=>scrollY<2,null,{timeout:2000});await expect(page.locator('.line-page>.card').first()).not.toHaveClass(/focus-role-line/);const nextHash=await page.evaluate(()=>location.hash);await page.evaluate(()=>{const p=document.querySelector('.line-page');p.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,pointerType:'touch',pointerId:42,clientX:220,clientY:220}));p.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,pointerType:'touch',pointerId:42,clientX:226,clientY:340}))});expect(await page.evaluate(()=>location.hash)).toBe(nextHash);await page.evaluate(()=>window.scrollTo(0,document.documentElement.scrollHeight));expect(await page.evaluate(()=>scrollY)).toBeGreaterThan(20);await page.evaluate(()=>{const p=document.querySelector('.line-page');p.dispatchEvent(new PointerEvent('pointerdown',{bubbles:true,pointerType:'touch',pointerId:43,clientX:110,clientY:260}));p.dispatchEvent(new PointerEvent('pointerup',{bubbles:true,pointerType:'touch',pointerId:43,clientX:310,clientY:264}))});await expect(page).toHaveURL(new RegExp(target.line));await page.waitForFunction(()=>scrollY<2,null,{timeout:2000});await page.waitForFunction(()=>document.querySelector('.line-page>.card')?.dataset.ownRole==='true');await expect(page.locator('.line-page>.card').first()).toHaveClass(/focus-role-line/);await page.evaluate(()=>window.scrollTo(0,document.documentElement.scrollHeight));expect(await page.evaluate(()=>scrollY)).toBeGreaterThan(20);await page.locator('[data-next]').click();await expect(page).toHaveURL(new RegExp(target.next));await page.waitForFunction(()=>scrollY<2,null,{timeout:2000})});
