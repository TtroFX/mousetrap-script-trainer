name: Chunking v1 QA

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rebuild-validate-and-publish:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Manual-only while canonical metric correction is being finalized.'
